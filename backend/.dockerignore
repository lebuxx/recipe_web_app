# Secrets - injected at runtime via env_file, not baked into the image
.env

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# local SQLite database (persistence is handled via the Docker volume)
*.db
*.db-journal
*.sqlite3
